#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//usr/local/bin/node" ]; then
  "$basedir//usr/local/bin/node"  "$basedir/../pistachio/compile.js" "$@"
  ret=$?
else 
  /usr/local/bin/node  "$basedir/../pistachio/compile.js" "$@"
  ret=$?
fi
exit $ret
